# Проверка стенда · 2026-09-25

Проверено с Python 3.12.14, `torch 2.6.0+cpu`, `streamlit 1.44.1`.

| Проверка | Результат |
| --- | --- |
| SHA-256 всех 6 checkpoint и 3 исходных модулей относительно мастер-архива | совпали |
| Четыре демонстрационных ряда относительно frozen `rollout_fresh.jsonl` | совпали |
| Сравнение исходного `run_r13.py` и пути, который вызывает Streamlit | PASS: 24 случая (4 сценария × 3 seed × 2 плана действий), belief, все вероятности и бинарные состояния |
| Допуск для вероятностей; для бинарных состояний | `2e-6`; точное совпадение |
| Валидация истории, запрет скрытых меток и выдача факта лишь при совпадении действия | 4 теста PASS |
| Headless Streamlit: исходный экран и принятие наблюдения | 0 исключений; история остаётся из 8 наблюдений |
| Прямой CLI inference | 2 шага, совместное распределение суммируется до 1 |

Чтобы повторить наиболее важную проверку, нужен оригинальный мастер-архив:

```bash
python scripts/verify_equivalence.py --archive /путь/к/мастер_архиву.zip
```

Проверка развёртывания на Streamlit Community Cloud не выполнялась; там нужно
установить зависимости из `requirements.txt` и выбрать Python 3.12.
